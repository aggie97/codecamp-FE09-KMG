import { add } from "../../pages/34-01-jest"; // 테스트 할 기능을 가져온다.

it("더하기 잘 되는지 테스트 해보기", () => {
  // 테스트 제목 (어떤 테스트인지 알 수 있게끔)
  const result = add(3, 5);
  expect(result).toBe(8);
  // 테스트할 기능을 가져와서 결과에 담은 다음, 메서드를 사용해서 예상 결과를 적어준다.
  // 메서드명이 정말 ... 직관적이다 ..
});

describe("나만의 테스트 그룹 만들기", () => {
  it("내가 하고 싶은 테스트 1", () => {});
  it("내가 하고 싶은 테스트 2", () => {});
  it("내가 하고 싶은 테스트 3", () => {});
});
