import ChildPage from "./02-child";

const MapElPage = () => {
  ["민겸", "다은", "정우"].forEach((el, index) => {
    console.log("el: ", el);
    console.log("index: ", index);
  });
  ["민겸", "다은", "정우"].forEach((el, index) => {
    console.log("el: ", el);
    console.log("index: ", index);
  });
  ["민겸", "다은", "정우"].forEach((el, index) => {
    console.log("el: ", el);
    console.log("index: ", index);
  });
  ["민겸", "다은", "정우"].forEach((el, index) => {
    console.log("el: ", el);
    console.log("index: ", index);
  });
  return <></>;
};

export default MapElPage;
