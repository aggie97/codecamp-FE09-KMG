function solution(seoul) {
  return `김서방은 ${seoul.indexOf("Kim")}에 있다`;

  // for(let i = 0, x = 0; i < seoul.length; i++){
  //     if(seoul[i] === "Kim") {
  //         x = i;
  //         return `김서방은 ${x}에 있다`;
  //     }
  // }
}
